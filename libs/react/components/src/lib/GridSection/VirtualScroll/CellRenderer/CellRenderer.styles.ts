import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  tableCellContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  displayMedia: {
    marginRight: 'var(--pm-S)',
  },
});
